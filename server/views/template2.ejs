<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Template 2 Blog Preview</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.1.2/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-white p-6">
    <div class="max-w-4xl mx-auto bg-gray-100 p-10 rounded-md shadow-md">
      <h1 class="text-3xl font-semibold mb-2 text-blue-700"><%= title %></h1>
      <p class="text-gray-800 mb-4"><%= content %></p>
      <div class="tags space-y-2">
        <% tags.forEach(tag => { %>
        <span
          class="block bg-blue-200 text-blue-900 px-4 py-2 rounded-lg text-sm font-bold"
        >
          <%= tag %>
        </span>
        <% }); %>
      </div>
    </div>
  </body>
</html>
