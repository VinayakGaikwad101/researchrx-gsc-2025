<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Template 3 Blog Preview</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.1.2/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <style>
      body,
      html {
        height: 100%;
        width: 100%;
        margin: 0;
        padding: 0;
      }
    </style>
  </head>
  <body class="bg-blue-50 w-full min-h-screen">
    <div class="w-full bg-white border-l-4 border-blue-500 p-8 md:p-12">
      <div class="w-full mx-auto px-4 md:px-8 lg:px-12">
        <h1 class="text-4xl font-bold text-blue-800 mb-4"><%= title %></h1>
        <p class="text-gray-900 text-base mb-6"><%= content %></p>
        <div class="tags flex flex-wrap gap-3">
          <% tags.forEach(tag => { %>
          <span
            class="bg-blue-300 text-blue-900 px-3 py-1 rounded-md text-sm font-semibold"
          >
            <%= tag %>
          </span>
          <% }); %>
        </div>
      </div>
    </div>
  </body>
</html>
