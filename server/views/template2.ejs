<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Template 2 Blog Preview</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.1.2/dist/tailwind.min.css" rel="stylesheet">
  <style>
    <%= style %>
  </style>
</head>
<body class="bg-gray-100 p-6">
  <div class="max-w-4xl mx-auto bg-white p-8 rounded-lg shadow-md">
    <h1 class="text-4xl font-semibold mb-6"><%= title %></h1>
    <p class="text-gray-800 mb-6"><%= content %></p>
    <div class="tags">
      <% tags.forEach(tag => { %>
        <span class="inline-block bg-green-200 rounded-full px-3 py-1 text-sm font-semibold text-green-700 mb-2"><%= tag %></span>
      <% }); %>
    </div>
    <div class="images mt-6">
      <% images.forEach(image => { %>
        <img src="<%= image %>" alt="Blog Image" class="mb-6 w-full">
      <% }); %>
    </div>
  </div>
</body>
</html>
