<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Template 2 Blog Preview</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.1.2/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <style>
      body,
      html {
        height: 100%;
        width: 100%;
        margin: 0;
        padding: 0;
      }
    </style>
  </head>
  <body class="bg-white w-full min-h-screen">
    <div class="w-full bg-gray-100 p-10">
      <div class="w-full mx-auto px-4 md:px-8 lg:px-12">
        <div class="flex flex-col md:flex-row">
          <div class="md:flex-1">
            <h1 class="text-3xl font-semibold mb-2 text-blue-700">
              <%= title %>
            </h1>
            <p class="text-gray-800 mb-4"><%= content %></p>
          </div>
          <div class="md:w-64 mt-6 md:mt-0 md:ml-8">
            <div class="tags space-y-2">
              <% tags.forEach(tag => { %>
              <span
                class="block bg-blue-200 text-blue-900 px-4 py-2 rounded-lg text-sm font-bold"
              >
                <%= tag %>
              </span>
              <% }); %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
