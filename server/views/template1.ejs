<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Template 1 Blog Preview</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.1.2/dist/tailwind.min.css" rel="stylesheet">
  <style>
    <%= style %>
  </style>
</head>
<body class="bg-gray-100 p-6">
  <div class="max-w-4xl mx-auto bg-white p-8 rounded-lg shadow-md">
    <h1 class="text-3xl font-bold mb-4"><%= title %></h1>
    <p class="text-gray-700 mb-4"><%= content %></p>
    <div class="tags">
      <% tags.forEach(tag => { %>
        <span class="inline-block bg-blue-200 rounded-full px-3 py-1 text-sm font-semibold text-blue-700 mb-2"><%= tag %></span>
      <% }); %>
    </div>
    <div class="images mt-4">
      <% images.forEach(image => { %>
        <img src="<%= image %>" alt="Blog Image" class="mb-4 w-full">
      <% }); %>
    </div>
  </div>
</body>
</html>
