<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Template 4 Blog Preview</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.1.2/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-green-50 p-10">
    <div
      class="max-w-4xl mx-auto bg-gray-50 p-12 rounded-xl border border-green-300 shadow-lg"
    >
      <h1 class="text-3xl font-extrabold text-green-700 mb-5"><%= title %></h1>
      <p class="text-gray-900 leading-relaxed mb-8"><%= content %></p>
      <div class="tags grid grid-cols-2 gap-4">
        <% tags.forEach(tag => { %>
        <span
          class="bg-green-200 text-green-900 px-4 py-1 rounded-lg text-sm font-medium"
        >
          <%= tag %>
        </span>
        <% }); %>
      </div>
    </div>
  </body>
</html>
