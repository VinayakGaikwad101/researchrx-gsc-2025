<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Template 4 Blog Preview</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.1.2/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <style>
      body,
      html {
        height: 100%;
        width: 100%;
        margin: 0;
        padding: 0;
      }
    </style>
  </head>
  <body class="bg-green-50 w-full min-h-screen">
    <div class="w-full bg-gray-50 p-12 border-t border-b border-green-300">
      <div class="w-full mx-auto px-4 md:px-8 lg:px-12">
        <h1 class="text-3xl font-extrabold text-green-700 mb-5">
          <%= title %>
        </h1>
        <p class="text-gray-900 leading-relaxed mb-8"><%= content %></p>
        <div class="tags grid grid-cols-2 sm:grid-cols-3 md:grid-cols-6 gap-4">
          <% tags.forEach(tag => { %>
          <span
            class="bg-green-200 text-green-900 px-4 py-1 rounded-lg text-sm font-medium text-center"
          >
            <%= tag %>
          </span>
          <% }); %>
        </div>
      </div>
    </div>
  </body>
</html>
